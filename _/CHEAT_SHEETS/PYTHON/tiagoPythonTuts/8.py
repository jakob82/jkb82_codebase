formatter="{} {} {} {}"

print(formatter.format(1, 13, 21, 55))
print(formatter.format("18", "21", "27", "32"))
print(formatter.format(True, False, False, True))
print(formatter.format(formatter, formatter, formatter, formatter))
print(formatter.format(
    "heyho",
    "welcome to my bario",
    "my name is dario",
    "sonology times private joke"
))